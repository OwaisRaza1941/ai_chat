import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF050816);
  static const backgroundSecondary = Color(0xFF0A1020);

  // Cards & Containers
  static const cardColor = Color(0xFF111827);
  static const cardColorLight = Color(0xFF141C2F);
  static const cardBorder = Color(0xFF1E293B);

  // Primary Accent
  static const primaryPurple = Color(0xFF7C3AED);

  // Secondary Accent
  static const primaryBlue = Color(0xFF5B8BFF);

  // Drawer
  static const drawerBg = Color(0xFF0B1120);

  // Success / Action
  static const primaryGreen = Color(0xFF00E6AB);

  // Chat Bubble
  static const sentMessage = Color(0xFF7C3AED);
  static const receivedMessage = Color(0xFF141C2F);

  // Text Colors
  static const whiteText = Color(0xFFF8FAFC);
  static const greyText = Color(0xFF94A3B8);
  static const lightGreyText = Color(0xFFCBD5E1);

  // Icons
  static const iconColor = Color(0xFFE2E8F0);
  static const iconInactive = Color(0xFF64748B);

  // Dividers
  static const divider = Color(0xFF1E293B);

  // Shadows & Glow
  static const purpleGlow = Color(0xFF7C3AED);
  static const blueGlow = Color(0xFF5B8BFF);
}
